<Window x:Class="Lavenders.UI.DeleteConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:fa="clr-namespace:FontAwesome.Sharp;assembly=FontAwesome.Sharp"
        Title="{DynamicResource DeleteEventTitle}"
        Width="410" SizeToContent="Height"
        WindowStyle="None" AllowsTransparency="True"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False" Background="Transparent"
        FontFamily="{DynamicResource LavendersFont}">
    <Border Margin="18" Padding="24" CornerRadius="18"
            Background="{DynamicResource LavenderCardBrush}"
            BorderBrush="{DynamicResource LavenderBorderBrush}"
            BorderThickness="1"
            Effect="{StaticResource SoftLavenderShadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Border Width="48" Height="48" CornerRadius="14"
                    Background="{DynamicResource LavenderMistBrush}"
                    HorizontalAlignment="Left">
                <fa:IconBlock Icon="Trash" FontSize="18"
                                Foreground="{DynamicResource LavenderDangerBrush}"
                                HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <StackPanel Grid.Row="1" Margin="0,16,0,22">
                <TextBlock Text="{DynamicResource DeleteEventQuestion}"
                           FontSize="19" FontWeight="SemiBold"/>
                <TextBlock Text="{DynamicResource DeleteCannotUndo}"
                           Margin="0,5,0,0" FontSize="13"
                           Foreground="{DynamicResource LavenderMutedTextBrush}"/>
            </StackPanel>
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="{DynamicResource Cancel}"
                        Style="{DynamicResource LavenderSecondaryButton}"
                        MinWidth="104" Height="42" Margin="0,0,10,0"
                        Click="Cancel_Click"/>
                <Button Content="{DynamicResource Delete}"
                        Style="{DynamicResource LavenderRippleButton}"
                        Background="{DynamicResource LavenderDangerBrush}"
                        MinWidth="104" Height="42"
                        Click="Delete_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
