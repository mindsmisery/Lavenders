<UserControl x:Class="Lavenders.UI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent" FontFamily="{DynamicResource LavendersFont}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="32" MaxWidth="760" HorizontalAlignment="Left">
            <TextBlock Text="{DynamicResource SettingsTitle}" FontSize="24" FontWeight="SemiBold"
                       Foreground="{DynamicResource LavenderDeepBrush}"/>
            <TextBlock Text="{DynamicResource SettingsSubtitle}" Margin="0,6,0,24"
                       Foreground="{DynamicResource LavenderMutedTextBrush}"/>

            <Border Style="{DynamicResource LavenderCardStyle}" Padding="24" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{DynamicResource SettingsCalendarSection}" FontSize="17" FontWeight="SemiBold"/>
                        <TextBlock Text="{DynamicResource SettingsShowWeekends}" Margin="0,6,24,0"
                                   Foreground="{DynamicResource LavenderMutedTextBrush}"/>
                    </StackPanel>
                    <CheckBox Grid.Column="1" IsChecked="{Binding ShowWeekends}"
                              Style="{DynamicResource LavenderToggleSwitch}"
                              AutomationProperties.Name="{DynamicResource SettingsShowWeekends}"
                              VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <Border Style="{DynamicResource LavenderCardStyle}" Padding="24" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="220"/></Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="{DynamicResource SettingsLanguage}" FontSize="17" FontWeight="SemiBold"/>
                        <TextBlock Text="{DynamicResource SettingsLanguageDescription}" Margin="0,5,24,0"
                                   Foreground="{DynamicResource LavenderMutedTextBrush}" TextWrapping="Wrap"/>
                    </StackPanel>
                    <ComboBox Grid.Column="1" ItemsSource="{Binding Languages}" SelectedItem="{Binding SelectedLanguage}"
                              VerticalAlignment="Center">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding DisplayName}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </Grid>
            </Border>

            <Border Style="{DynamicResource LavenderCardStyle}" Padding="24">
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="220"/></Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="{DynamicResource SettingsTheme}" FontSize="17" FontWeight="SemiBold"/>
                        <TextBlock Text="{DynamicResource SettingsThemeDescription}" Margin="0,5,24,0"
                                   Foreground="{DynamicResource LavenderMutedTextBrush}" TextWrapping="Wrap"/>
                    </StackPanel>
                    <ComboBox Grid.Column="1" ItemsSource="{Binding Themes}" SelectedItem="{Binding SelectedTheme}"
                              VerticalAlignment="Center">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding DisplayName}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </Grid>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
